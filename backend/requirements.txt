fastapi>=0.141.1
uvicorn[standard]>=0.52.4  # [standard] bundles websockets/uvloop — WS endpoints break without it
sqlalchemy>=2.0.52
pyjwt>=2.13.0        # >=2.13.0 fixes PYSEC-2026-175/176/177/178/179 (JWK/HMAC confusion, PyJWKClient SSRF)
pydantic>=2.13.5
liboqs-python>=0.16.0   # ML-DSA-44 (FIPS 204) in-process; compiles liboqs C — needs cmake + a C compiler
python-multipart>=0.0.32
httpx>=0.28.1
slowapi==0.1.10
redis>=8.1.0              # optional shared/persistent rate-limit storage (set REDIS_URL); falls back to in-memory
alembic>=1.19.1
psycopg[binary]>=3.3.4  # PostgreSQL driver (SQLAlchemy URL scheme: postgresql+psycopg://)
pywebpush>=2.5.0
