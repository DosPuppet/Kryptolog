fastapi>=0.128
uvicorn[standard]>=0.40  # [standard] bundles websockets/uvloop — WS endpoints break without it
sqlalchemy>=2.0.46
pyjwt>=2.13.0        # >=2.13.0 fixes PYSEC-2026-175/176/177/178/179 (JWK/HMAC confusion, PyJWKClient SSRF)
pydantic>=2.12
liboqs-python>=0.12   # ML-DSA-44 (FIPS 204) in-process; compiles liboqs C — needs cmake + a C compiler
python-multipart>=0.0.22
httpx>=0.27
slowapi==0.1.9
redis>=5              # optional shared/persistent rate-limit storage (set REDIS_URL); falls back to in-memory
alembic>=1.13
psycopg[binary]>=3.2  # PostgreSQL driver (SQLAlchemy URL scheme: postgresql+psycopg://)
pywebpush>=1.14
